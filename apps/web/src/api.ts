import type { Account, Listing, OperationRecord, PriceTarget, SearchCriteria } from '@preco-certo/domain';
async function request<T>(path:string,init?:RequestInit):Promise<T>{const res=await fetch(path,{...init,headers:{'Content-Type':'application/json',...init?.headers}});const data=await res.json();if(!res.ok)throw Object.assign(new Error(data.message??'Falha na API'),{data});return data}
export const api={accounts:()=>request<Account[]>('/api/accounts'),search:(criteria:SearchCriteria)=>request<Listing[]>('/api/search',{method:'POST',body:JSON.stringify(criteria)}),history:()=>request<OperationRecord[]>('/api/history'),execute:(criteria:SearchCriteria,targets:PriceTarget[])=>request<{operation:OperationRecord}>('/api/operations/execute',{method:'POST',body:JSON.stringify({criteria,targets,approved:true})})};
