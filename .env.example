# Somente backend. Nunca prefixe segredos com VITE_.
HOST=127.0.0.1
PORT=3333
DATABASE_PATH=./data/preco-certo.sqlite
APP_USER=operador-local

# Integração real permanece inativa por padrão.
ML_CLIENT_ID=
ML_CLIENT_SECRET=
ML_REDIRECT_URI=
ML_ENCRYPTION_KEY=
ML_READ_ENABLED=false
ML_WRITE_ENABLED=false
# Apenas IDs de usuários de TESTE, separados por vírgula. Vazio por padrão.
ML_TEST_USER_ALLOWLIST=

